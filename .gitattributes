# Auto-detect text files and normalise line endings
* text=auto

# Shell scripts → LF (Linux/Mac)
*.sh text eol=lf

# PowerShell scripts → CRLF (Windows)
*.ps1 text eol=crlf

# Keep binary files as-is
*.png binary
*.jpg binary
*.ico binary
